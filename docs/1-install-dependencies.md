# Install project dependencies

```zsh
# install express
yarn add express

# install dev dependencies
yarn add -D \
  @types/express \
  @types/jest \
  @types/node \
  @types/supertest \
  jest \
  supertest \
  ts-jest \
  ts-node-dev \
  typescript
```
